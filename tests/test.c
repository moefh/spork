
#include "test.h"

#define SIZE     256
#define ROUND(x) (((x+SIZE)/SIZE)*SIZE)

#define X(a,b,...)  a(b,__VA_ARGS__)
#define Y s->f

int main(void)
{
  Y(1);
  //X(f,2,3);
  printf("Hello, world!\n");
  a +++++ b;
  while (i --> 0);
  return 0;
}
